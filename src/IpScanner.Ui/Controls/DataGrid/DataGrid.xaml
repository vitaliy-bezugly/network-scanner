<UserControl
    x:Class="IpScanner.Ui.Controls.DataGrid.DataGrid"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:IpScanner.Ui.Controls.DataGrid"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:core="using:Microsoft.Xaml.Interactions.Core"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <!-- Results list -->
    <controls:DataGrid x:Name="ScannedDevicesDataGrid" CanBeScrollAnchor="True" ScrollViewer.VerticalScrollMode="Enabled" 
        ScrollViewer.IsVerticalRailEnabled="True" ScrollViewer.VerticalScrollBarVisibility="Visible"
        ItemsSource="{Binding ItemsSource, ElementName=RootControl}" 
        SelectedItem="{Binding SelectedItem, ElementName=RootControl}"
        Visibility="{Binding Visibility, ElementName=RootControl}"
        PreparingCellForEdit="FavoritesDevicesDataGrid_PreparingCellForEdit">
    </controls:DataGrid>
</UserControl>
