<Page
    x:Class="IpScanner.Ui.Pages.ProgressBar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="3" VerticalAlignment="Top"> 
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="0" Margin="0,0,5,0" Text="{x:Bind ViewModel.ProgressString, Mode=OneWay}"/>
        <ProgressBar Grid.Column="1" IsIndeterminate="False" Height="6"
              Value="{x:Bind ViewModel.CountOfScannedIps, Mode=TwoWay}" 
              Maximum="{x:Bind ViewModel.TotalCountOfIps, Mode=TwoWay}"/>
    </Grid>
</Page>
